<Window x:Class="VRChatUserObserver.TFADialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:VRChatUserObserver"
        mc:Ignorable="d"
        Title="Login" 
        Height="Auto" Width="400"
        SizeToContent="Height"
        ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
		
        <Label Content="Code :" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
        <TextBox x:Name="code" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="{Binding UserName}" VerticalAlignment="Center" Grid.Row="0" Grid.Column="1" Margin="5,0,5,0"/>

        <Button Content="Verify" HorizontalAlignment="Right" Margin="0,5,5,5" VerticalAlignment="Center" Grid.Row="1" Grid.Column="1" Width="100" Click="OnVerify"/>
    </Grid>
</Window>
